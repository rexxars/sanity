module.exports = assets => {
  // {documentId, path, url, type}
  // Can we uset sets?
  // Loop over assets, build unique array of type/url combo
  // Start uploading/checking with a given concurrency
  // For every completed item, make a backref from type/url => doc id
  // Once all uploads are complete,
}
